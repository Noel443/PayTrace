# AGENT.md

## 项目目标

这是一个以本地运行和演示为主的 PayTrace 工作台。实现功能时优先保持现有行为稳定，使用简单、容易维护的方案。

## 技术范围

- 前端：现有的 HTML、CSS 和原生 JavaScript。除非确有必要，不新增前端框架或构建工具。
- 后端：Node.js，遵循项目现有的 `.mjs` 模块和本地 HTTP 服务方式。
- 数据：优先沿用现有的浏览器存储和 `data/` 本地文件方案，不为了小功能引入数据库。
- 只有在需求明确且现有技术无法满足时，才增加其他技术，并在提交说明中解释原因。

## 实现原则

1. 先阅读相关代码和 README，理解现有数据格式、接口和页面流程，再修改代码。
2. 代码尽可能简单直白：小函数、清晰命名、少量抽象，避免为了未来假设提前设计复杂架构。
3. 一个功能只改动必要文件，保持现有接口和用户数据兼容；需要变更格式时提供向后兼容读取或明确迁移逻辑。
4. 前端输入必须校验，后端接口必须校验方法、参数、路径和大小；错误信息要对用户有用，但不能泄露密码、API Key、令牌或内部堆栈。
5. 涉及文件、命令、SSH、模型调用或外部输入时，使用白名单、固定参数和超时；禁止拼接未经校验的 shell 命令。
6. 不把密钥、密码、真实日志、个人信息或本机绝对路径写入代码、测试样例、日志或提交内容。
7. 保留现有用户数据和工作区修改。发现不确定的行为时，选择兼容方案并在说明中记录假设。

## 数据库与 SQL 规范

如果功能接入 MySQL 数据库，必须同时提交对应的 SQL 文件，统一保存到项目根目录的 `sql/` 目录，不允许只在代码或聊天内容中描述建表和变更。每个数据库变更使用独立、可追溯的文件，文件名使用：

```text
YYYYMMDDHHmmss_short_description.sql
```

SQL 文件开头必须写明变更时间、变更目的、涉及的表以及执行注意事项。每张表都必须在 SQL 注释或配套文档中说明“这张表是做什么的”、数据归属和主要字段用途；不能创建用途不清楚的表或字段。

表结构默认遵循以下约定：

- 使用 MySQL 8.x 兼容语法，字符集使用 `utf8mb4`，排序规则按项目现有 MySQL 版本选择，存储引擎使用 `InnoDB`。
- 需要记录时间的业务表至少包含 `created_at` 和 `updated_at`，使用 `DATETIME` 或项目已统一的时间类型，并明确由数据库或应用负责写入和更新；涉及删除替代方案时可使用 `deleted_at` 软删除字段，但不得借此自动删除数据。
- 主键、唯一约束、外键、索引和字段是否允许 `NULL` 必须根据业务用途明确设置；金额、数量和状态字段使用适合业务精度的类型，不使用模糊字符串代替。
- SQL 中使用反引号、`AUTO_INCREMENT`、`CURRENT_TIMESTAMP` 等 MySQL 语法时，确保目标 MySQL 版本支持；不得混用 PostgreSQL、SQLite 或其他数据库的专有语法。
- SQL 参数由后端使用预处理语句绑定，禁止把用户输入直接拼接进 SQL；表名、列名等动态标识符只允许来自固定白名单。

新增表、字段、索引或约束时，必须保留可重复检查的迁移记录，并在代码中同步更新读写逻辑。不得在迁移文件中使用 `DROP TABLE`、`DROP DATABASE`、无条件 `DELETE`、无条件 `TRUNCATE` 或其他会丢失数据的语句；需要调整结构时优先采用新增字段、兼容读取和可回滚的迁移方式。测试数据也不得指向真实数据库或清空已有数据。

## 严格禁止删除和破坏性操作

AI 绝对禁止执行任何删除操作，包括但不限于：

- 删除文件、目录、数据、配置、记录、代码或用户内容；
- 使用 `rm`、`unlink`、`rmdir`、`git clean`、`git reset --hard`、丢弃工作区修改的 checkout/restore 等命令；
- 覆盖或清空用户已有文件、数据库、`data/` 内容、浏览器存储或 Git 历史；
- 为了测试而清理目录、重置环境或删除依赖。

如果需求看起来需要删除，AI 只能提出说明和非破坏性的替代方案，不能自行执行。需要由用户明确手工完成的破坏性步骤，应单独标注风险、影响范围和备份建议。除非用户另行明确授权，AI 也不执行提交历史改写、强制推送或其他不可逆操作。

## 安全规范

- 默认只监听本机地址；不得无故扩大网络暴露范围。
- 所有外部输入都视为不可信，防止路径穿越、命令注入、任意文件读取、SSRF、XSS 和敏感信息回显。
- 本地敏感配置使用最小权限；接口返回值默认脱敏，密码和密钥只允许在服务端使用。
- SSH 必须校验主机身份，设置连接和读取超时，限制输出大小；不得通过关闭校验来绕过错误。
- 不执行用户项目代码，不自动拉取、推送或修改其他 Git 仓库。
- 发现疑似安全问题时先停止相关改动，说明风险和影响，再给出修复方案。

## 验证要求

完成修改后运行与改动相关的检查；本项目通常使用：

```sh
npm test
```

涉及数据库时，还要在隔离的测试库中使用目标 MySQL 版本执行 `sql/` 下本次新增或变更的脚本，检查建表、索引、约束、时间字段和回滚影响；验证不得连接生产库或修改已有真实数据。

如测试无法运行，说明原因、已完成的替代检查和剩余风险。不要为了让测试通过而降低校验、隐藏错误或删除测试。

## 功能完成后的提交评论

每完成一个功能，都要在回复末尾输出一段可用于 Git commit 的提交评论，即使没有执行 `git commit` 也要提供。格式如下：

```text
commit: <type>(<scope>): <一句话说明>

- 做了什么
- 为什么这样做
- 验证方式：<命令或检查结果>
```

`type` 使用 `feat`、`fix`、`refactor`、`docs`、`test` 或 `chore`。评论只描述本次实际改动，不虚构未完成的工作。除非用户明确要求，AI 不自动创建 commit；如果用户要求提交，先检查变更范围和测试结果，并再次确认没有包含敏感信息或不相关修改。
