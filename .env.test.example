# 测试环境配置。复制为 .env.test，或直接使用项目根目录 .env
PORT=19527
# 同一 MySQL 库内切换环境无需填写；仅独立部署跳转时填写实际地址。
PAYTRACE_COUNTERPART_URL=
PUBLIC_ORIGINS=

# 默认读取 config/database.json；指定其他应用库时可设置下面一项。
# DATABASE_CONFIG_FILE=config/database.test.json

AI_PROVIDER=compatible
AI_BASE_URL=
AI_MODEL=
AI_API_KEY=
